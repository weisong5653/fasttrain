<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.PathDao">
    <insert id="savePath">
        insert into path(id, order_id, origin, terminus) values (
        #{path.id},
        #{path.orderId},
        #{path.origin},
        #{path.terminus}
        )
    </insert>
</mapper>